/*package com.test.appium;

import java.io.IOException;

public class Start {
    @SuppressWarnings("deprecation")
	public static void main(String[] args) {
        try {
            // Path to the batch file
            String batchFilePath = "C:\\Program Files\\MuMuVMMVbox\\Sstart.bat";

            // Run the batch file
            Process process = Runtime.getRuntime().exec(batchFilePath);

            // Wait for the batch file to finish executing
            //process.waitFor();

            System.out.println("Batch file executed successfully.");

        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}*/
